def fatorial(n):
    """
    Calcula o fatorial de um número usando recursão.
    Exemplo: fatorial(5) = 5 * 4 * 3 * 2 * 1 = 120
    """
    if n == 0 or n == 1:
        return 1
    else:
        return n * fatorial(n - 1)

# Execução principal
if __name__ == "__main__":
    try:
        numero = int(input("Digite um número inteiro para calcular o fatorial: "))
        if numero < 0:
            print("Fatorial não definido para números negativos.")
        else:
            resultado = fatorial(numero)
            print(f"Resultado: {numero}! = {resultado}")
    except ValueError:
        print("Erro: Por favor, digite um número inteiro válido.")



*Código Python que calcula o fatorial de um número usando recursão, em https://www.online-python.com/
